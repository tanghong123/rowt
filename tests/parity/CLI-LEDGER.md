# rowt-rs readiness

Generated by `parity cli-ledger`. rowt-rs is built alongside the shell;
this is how much of it could stand in, and how much of that is proven.

Every arm RUNS through rowt-rs — the ones it does not answer itself are
handed to bin/rowt (PORTING.md §6.6), so the question this table answers
is not 'does it work' but 'whose code ran, and what proves it'.

  native   rowt-rs answers it        legacy   falls through to the shell
  cli-diff byte-compared vs bash    boot-test exercised against real sing-box
  (passthrough) = bash vs bash: proves the front door is transparent,
                  not that anything was ported

| command | runs in | proven by |
|---|---|---|
| `_complete` | legacy | — |
| `audit` | **native** | cli-diff |
| `block` | **native** | cli-diff |
| `completion` | **native** | cli-diff |
| `config` | legacy | — |
| `connections` | **native** | cli-diff |
| `conns` | **native** | cli-diff |
| `corp` | **native** | cli-diff |
| `direct` | **native** | cli-diff |
| `down` | **native** | boot-test |
| `escape` | **native** | cli-diff |
| `explain` | **native** | cli-diff |
| `fetch` | legacy | — |
| `metrics` | legacy | — |
| `mon` | **native** | — |
| `monitor` | **native** | — |
| `onboard` | legacy | — |
| `ping` | legacy | — |
| `probe` | legacy | — |
| `proxy` | **native** | cli-diff |
| `reload` | **native** | boot-test |
| `render` | **native** | boot-test |
| `report` | legacy | — |
| `restart` | **native** | boot-test |
| `route` | **native** | — |
| `router` | **native** | cli-diff + boot-test |
| `run` | legacy | — |
| `server` | legacy | — |
| `shell-init` | **native** | cli-diff |
| `skill` | legacy | — |
| `status` | **native** | cli-diff |
| `sub` | legacy | — |
| `uninstall` | legacy | — |
| `up` | **native** | boot-test |
| `use` | legacy | — |
| `vm` | legacy | — |
| `watch` | legacy | cli-diff (passthrough) |

**21 of 37 command arms answered natively**; the other 16 run in the shell.

Partial arms — native for some sub-commands, legacy for the rest.
`native()` in crates/rowt-cli/src/main.rs is the authority:

            "escape" | "corp" | "block" => matches!(
                sub,
                "" | "list" | "dump" | "add" | "rm" | "remove" | "clear" | "import"
                    | "errors" | "stats" | "log"
            ),
            "proxy" => matches!(sub, "" | "status" | "check" | "env"),
            "router" => matches!(sub, "" | "up" | "down" | "restart" | "status"),
