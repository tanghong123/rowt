# Synthetic parity fixture — the hotspot lane: captive-portal hosts the OS
# reaches WITHOUT the proxy. Mixed case, an exact entry and a CIDR on purpose:
# the bypass list is lowercased and shaped from all three.
Portal.Example
domain:login.hotspot.example
203.0.113.0/24
intranet.corp.example
