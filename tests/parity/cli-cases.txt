# Commands rowt-rs claims. Each must produce byte-identical stdout and leave the
# lane files identical to what the shell leaves.

explain example.com
# `route` is the same arm under a second name, and the ledger counts names.
route example.com
explain intranet.corp.example
explain ads.example
explain unlisted.example
explain 198.51.100.7
explain 10.99.4.4
explain https://Example.COM:8443/a/b
escape list
corp list
block list
escape dump
escape add new.example
corp add example.com
escape rm example.com
escape rm absent.example
block clear

# local mode: the escape lane's verdicts must read DIRECT, with the reason saying why
PARITY_SCENARIO=render-local explain example.com
PARITY_SCENARIO=render-local explain sub.example.net
PARITY_SCENARIO=render-local explain ads.example
PARITY_SCENARIO=render-local explain intranet.corp.example
PARITY_SCENARIO=render-local-precedence explain api.foo.com
PARITY_SCENARIO=render-local escape list

# newly ported: version, the proxy read paths, and lane import
version
proxy env
proxy env --off
proxy check
proxy status
escape import tests/parity/fixtures/import-list.txt
corp import tests/parity/fixtures/import-list.txt

# static text and log readers, extracted from the shell at build time
shell-init
completion zsh
completion bash
audit
audit path
audit all
status
PARITY_SCENARIO=render-local status

# `render` itself. What it WRITES is `render-diff`'s question over the whole
# scenario matrix; what these two add is the command around it — the summary,
# the exit status, and which side of the guard a given config falls on. The
# empty-servers case is the one that matters: the shell asks whether
# servers.json has bytes, so `[]` renders rather than refusing (PORTING.md §6.7).
render
PARITY_SCENARIO=render-empty-servers render

# help routing: the systematic '<cmd> --help' as well as 'help <cmd>'
--help
help
help nosuchcommand
escape --help
watch --help

# Every page in the registry, one case each. Not thoroughness for its own sake:
# the pages share one renderer, and it processes the heredoc escapes the shell
# would. A page that gets that wrong still LOOKS like a page — `\$SHELL` coming
# out as the value of $SHELL rather than the literal reads perfectly well — so
# nothing but a comparison catches it. Seven cases used to stand for 32 pages.
help audit
help block
help completion
help config
help connections
help corp
help direct
help down
help escape
help explain
help fetch
help metrics
help monitor
help onboard
help ping
help probe
help proxy
help reload
help render
help report
help restart
help router
help run
help server
help shell-init
help skill
help status
help sub
help uninstall
help up
help use
help version
help vm
help watch

# lane sub-arms — a gated arm is not a gated COMMAND. These are the forms
# `escape|corp|block|direct` take that are not list edits.
escape errors
corp errors 1h
block errors
direct errors
# `log` is `tail -f`: both sides are expected to be killed by the alarm, so
# it gets a short one rather than a minute of proving they hang alike.
PARITY_TIMEOUT=3 direct log
PARITY_TIMEOUT=3 escape log
escape errors nonsense
direct nonsense

# live-state readers
connections
conns escape

# with the router up: the clash API answers, so the aggregation, the humanised
# byte columns and both sort keys are reachable — as are status's proxy probe
# and explain's live line.
PARITY_SCENARIO=router-up connections
PARITY_SCENARIO=router-up connections escape
PARITY_SCENARIO=router-up connections direct
PARITY_SCENARIO=router-up conns block
PARITY_SCENARIO=router-up connections nosuchlane
PARITY_SCENARIO=router-up status
PARITY_SCENARIO=router-up explain example.com
PARITY_SCENARIO=router-up router status

# the tab-completion helper, which is driven by the same registry as `usage`
_complete
_complete escape
_complete up
_complete use
_complete ping
_complete help
_complete router
_complete nosuchcommand
_complete escape add

# server / sub: the read arms.
server
server list
server dump
sub
sub list
sub dump
server nosucharm
sub nosucharm
PARITY_SCENARIO=router-up server list

# server / sub: the arms that WRITE. What these prove is on disk, not on stdout
# — manual.json, subs.txt, the rebuilt servers.json and the `selected` line of
# state — which is why cli-diff compares the config tree (content and mode) and
# not just the two output streams.
#
# The subscriptions in the `pool-seeded` fixture point at a closed local port,
# so every rebuild here takes the fetch-failed path. That is the only
# subscription path the two implementations can be compared on: bash fetches
# with urllib inside vless-parse.py and rowt-rs with curl, so a SUCCESSFUL fetch
# would differ in the argv trace whatever the bodies were. The parse side of a
# real subscription body is covered by `parity vless-diff`.
server add vless://00000000-0000-4000-8000-000000000009@203.0.113.20:443?security=tls&sni=delta.example#delta
server add
server add notalink
server add vless://00000000-0000-4000-8000-000000000009@203.0.113.20:443#d1 vless://00000000-0000-4000-8000-000000000009@203.0.113.20:443#d2
# Re-importing something the manual set already has must be idempotent on disk,
# not merely deduped again in servers.json on the next rebuild.
PARITY_SCENARIO=pool-seeded server add vless://00000000-0000-4000-8000-000000000001@203.0.113.10:443#alpha-again
server rm alpha
server rm nosuchtag
PARITY_SCENARIO=pool-seeded server rm alpha
PARITY_SCENARIO=pool-seeded server rm alpha gamma
PARITY_SCENARIO=pool-seeded server rm alpha nosuchtag
server clear
PARITY_SCENARIO=pool-seeded server clear
# No `router-up` case here, and none is possible: the scenario makes the router
# "running" by writing the HARNESS's own pid into host.pid, so the reload that
# every pool edit ends with would stop the gate itself. The reload path is
# shared with `corp sync`, which is unreachable in this gate for the same reason.
sub add http://127.0.0.1:9/feed-three
sub add
PARITY_SCENARIO=pool-seeded sub add http://127.0.0.1:9/feed-three
# `sort -u` — a URL already in the list must not become a second line.
PARITY_SCENARIO=pool-seeded sub add http://127.0.0.1:9/feed-one
sub rm 1
sub update
PARITY_SCENARIO=pool-seeded sub rm 1
# The shell applies the arguments one at a time to the SHRINKING file, so these
# two numbers do not mean what they look like. Bug-for-bug (PORTING.md §6.7).
PARITY_SCENARIO=pool-seeded sub rm 1 2
PARITY_SCENARIO=pool-seeded sub rm feed-two
PARITY_SCENARIO=pool-seeded sub rm 99
PARITY_SCENARIO=pool-seeded sub update
PARITY_SCENARIO=pool-seeded sub clear
sub clear
# A manual set that is not a JSON array at all: `jq -s add` fails and the
# rebuild silently continues without it, so servers.json is rebuilt from the
# subscriptions alone rather than the command failing.
PARITY_SCENARIO=pool-invalid server list
PARITY_SCENARIO=pool-invalid server add vless://00000000-0000-4000-8000-000000000009@203.0.113.20:443#delta
PARITY_SCENARIO=pool-invalid server clear
# An array whose elements are not all objects. This one does NOT degrade
# quietly: the Python calls .get on a string, which is an AttributeError rather
# than a ValueError, so it escapes its own handler — the rebuild dies, and the
# audit log gets an ABORT with no END. Both halves of that are compared.
PARITY_SCENARIO=pool-corrupt server list
PARITY_SCENARIO=pool-corrupt server add vless://00000000-0000-4000-8000-000000000009@203.0.113.20:443#delta
PARITY_SCENARIO=pool-corrupt server clear

# server import / sub import — the other clients' accumulate → edit → apply
# workflow. The sandbox HOME contains no VPN client at all, which is itself the
# case worth having: `--detect` must find nothing on both sides, and each
# `--from` must fail (or produce an empty review) the same way. What a client
# tree full of data yields is `foreign-diff`'s and `sr-diff`'s question, over
# 1,200 synthetic installs each; `yq` is not on the sandbox PATH, so the Clash
# YAML path could not be compared here even with a fixture.
server import --detect
sub import --detect
server import
server import --from
server import --from nosuchclient
server import --nosuchflag
server import --apply
server import --from shadowrocket
server import --from clash-verge
server import --from v2box
server import --from flclash
# `--path` is appended to whichever dumper was picked, and sr-import.py has no
# such option — so this combination always fails. Bug-for-bug (PORTING.md §6.7).
server import --from shadowrocket --path /nonexistent
server import --from v2box --path /nonexistent
# The half that WRITES: a review file someone has already edited, applied to the
# pool. Servers into manual.json deduped by endpoint, subscription URLs appended
# and sorted, proxy domains merged into the escape lane under a marker — one of
# them a duplicate of a domain the lane already has, so the dedupe is exercised
# rather than assumed.
PARITY_SCENARIO=import-review server import --apply
PARITY_SCENARIO=import-review sub import --apply
PARITY_SCENARIO=import-review server import --detect
# An empty list dumps a bare NEWLINE — `printf '%s\n' ""` — not nothing. Each
# case gets a fresh sandbox, so emptiness has to come from a scenario rather
# than from a `clear` in the case before it.
PARITY_SCENARIO=lanes-empty block dump
PARITY_SCENARIO=lanes-empty escape dump
PARITY_SCENARIO=lanes-empty escape list
PARITY_SCENARIO=lanes-empty sub dump
PARITY_SCENARIO=lanes-empty sub list
PARITY_SCENARIO=lanes-empty explain anything.example

# use / config / metrics / router
PARITY_SCENARIO=lanes-empty use
use alpha
use auto
use nosuchtag
PARITY_SCENARIO=render-local use alpha
# A name nobody documents: error to stderr, a blank line and the usage to
# stdout, exit 1 — and, because the shell's catch-all calls `exit` from inside
# the audited region, a BEGIN with no END in the trail. All four compared.
nosuchcommand
config
config list
config nosucharm

# `config import` — unpack a bundle over the config directory. Every refusal
# comes before the prompt, which is why they are cases at all:
config import
config import /nonexistent/bundle.tgz
config import --nosuchflag
config import tests/parity/fixtures/import-list.txt
config import tests/parity/fixtures/not-a-bundle.tgz
#
# …and the prompt itself IS comparable, because the harness runs every case in
# its own session with no controlling terminal: `read </dev/tty` fails, the
# shell treats that as "no", and both sides must print the same refusal and
# exit 1. That is the real behavior on a pipe or in cron, not a test artifact.
config import tests/parity/fixtures/config-bundle.tgz
#
# The bundle carries all four secret-bearing files at 0644, so `-y` compares
# what the shell chmods after extracting as well as what it extracts — and then
# the re-render, which the import runs quietly and reports one line about.
config import tests/parity/fixtures/config-bundle.tgz -y
config import -y tests/parity/fixtures/config-bundle.tgz
# The flag loop keeps the LAST path it saw, so this imports the second one.
config import tests/parity/fixtures/not-a-bundle.tgz tests/parity/fixtures/config-bundle.tgz -y
# Over a config with nothing in its lanes: the bundle's lists replace them.
PARITY_SCENARIO=lanes-empty config import tests/parity/fixtures/config-bundle.tgz -y
#
# A bundle whose servers.json is an empty array. It renders — `render` guards on
# the FILE having bytes, not on there being a server in it — which is why the
# import reports ✓ rather than the fresh-box advice. Bug-for-bug, PORTING.md
# §6.7, and the reason there is now a `render` case for the same fixture below.
config import tests/parity/fixtures/bundle-no-servers.tgz -y
metrics
metrics status
metrics path
metrics top
metrics query
metrics nosucharm
router status
PARITY_TIMEOUT=3 router log

# ping / run
PARITY_UNORDERED=1 ping
PARITY_UNORDERED=1 PARITY_SCENARIO=router-up ping
PARITY_SCENARIO=router-up ping alpha
PARITY_UNORDERED=1 PARITY_SCENARIO=lanes-empty ping
run

# skill / report / uninstall. `report` probes the live machine, so what the gate
# checks is the SHAPE — section order and every probe's argv — not the diagnosis.
skill
skill status
skill nosucharm
report
PARITY_SCENARIO=router-up report
# The usage lines a die() emits reach the AUDIT log via ABORT, so a message that
# only bash's stderr would have shown is still compared for the mutating arms.
proxy nosucharm
escape nosucharm
router nosucharm
metrics query
shell-init nosucharm
fetch nosucharm
PARITY_UNORDERED=1 probe
PARITY_UNORDERED=1 PARITY_SCENARIO=lanes-empty probe
vm status
vm nosucharm

# corp sync / suggest, and the watchdog's read arms
corp sync --dry-run
corp sync --no-reload --quiet
corp suggest
corp sync --nosuchflag
watch
watch status
watch nosucharm
onboard
PARITY_SCENARIO=router-up onboard
PARITY_SCENARIO=lanes-empty onboard

# `uninstall` — it was claimed native and gated by nothing, which the ledger
# has been saying all along. It is a COMPOSITION of arms already gated here
# (revert, proxy off, watch uninstall) plus the rc-file edit, so what these add
# is the order it does them in — visible in the argv trace — and that the two
# implementations agree about what is left behind. Effects stay inside the
# sandbox: every command it calls out to is shimmed, and the rc files and the
# config directory it touches are the sandbox's own.
uninstall
# `--purge` removes the config directory it just finished writing to, audit log
# included. Both sides must agree on that too.
uninstall --purge

# The lifecycle arms — up, down, reload, restart. The ledger called these
# "proven by boot-test", and the boot test does run them: against a REAL
# sing-box, checking that the tunnel comes up. What it never does is compare
# them to the shell, and every one of them diverged when first asked to.
#
# They are safe here for the same reason the rest of the sweep is: sing-box is
# the fixture's fake, `networksetup` and `limactl` are shims, and the only
# process either side actually spawns dies immediately — which is precisely
# what makes the failure path the one being compared. `up` is the slowest case
# in the file (two 5s readiness waits and a settle, per side) and worth it: the
# failure path is where the proxy gets cleared, the intent gets stamped and the
# exit status is decided.
#
# NOTE the trap that keeps these ungatable under `PARITY_SCENARIO=router-up`:
# that scenario writes the HARNESS's own pid into host.pid so the router reads
# as running, and every one of these arms would then kill the gate. It is the
# same documented hazard as `reload`, and it applies to `down`, `restart`,
# `up` and `uninstall` too — not just the two it was first written for.
up host
up local
# Bare `up` chooses its own mode: no target named means `_direct_reaches_escape`
# curls the GFW canaries (the shim answers, so this is deterministic here) and
# then `probe` decides. Probe fans out one resolver call per server, so the
# trace is in whatever order the scheduler produced — compared as a SET, like
# every other case that forks.
PARITY_UNORDERED=1 up
down
reload
restart

# proxy on/off — safe to gate because BOTH sides funnel every system-proxy
# write through one guarded choke point, and the sandbox has no service anyway.
proxy off
proxy on
PARITY_SCENARIO=proxy-on proxy off
PARITY_SCENARIO=router-up proxy on
